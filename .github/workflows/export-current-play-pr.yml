name: Export current Play PR source

on:
  pull_request:
    branches:
      - master

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/minimal-play-26-1-2
      - uses: actions/upload-artifact@v4
        with:
          name: rom-current-play-branch
          path: |
            Cargo.toml
            Cargo.lock
            README.md
            crates
            docs
          if-no-files-found: error
