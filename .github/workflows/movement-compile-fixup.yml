name: Movement compile fixup

on:
  pull_request:
    types: [synchronize, reopened]

permissions:
  contents: write

jobs:
  fix:
    if: ${{ github.head_ref == 'feature/vanilla-parity-foundation' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/vanilla-parity-foundation
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Fix movement integration compile errors
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          python scripts/fix_movement_compile.py
          cargo fmt --all
          git add crates/ferrum-server/src/main.rs crates/ferrum-server/src/play_runtime.rs
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "Fix movement integration compile errors"
          git push origin HEAD:feature/vanilla-parity-foundation
